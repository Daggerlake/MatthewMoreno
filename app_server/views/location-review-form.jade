extends layout

block content
  .row.page-header
    .col-lg-12
      h1= pageHeader.title
  .row
    .col-xs-12.col-md-6
      // when form is submitted, it will make a POST request to
      // URL of current page
      form#addReview.form-horizontal(action="", method="post", role="form")
        - if (error == "val") 
          .alert.alert-danger(role="alert") All fields required, please try again
        .form-group
          // input field for reviewer to leave name
          label.col-xs-10.col-sm-2.control-label(for="name") Name
          .col-xs-12.col-sm-10
            input#name.form-control(name="name")
        .form-group
          label.col-xs-10.col-sm-2.control-label(for="rating") Rating
          .col-xs-12.col-sm-2
            // dropdown select box for rating 1 to 5
            select#rating.form-control.input-sm(name="rating")
              option 5
              option 4
              option 3
              option 2
              option 1
        .form-group
          label.col-sm-2.control-label(for="review") Review
          .col-sm-10
            // text area for content of review
            textarea#review.form-control(name="review", rows="5")
        // submit button for form
        button.btn.btn-default.pull-right Add my review
    .col-xs-12.col-md-4
